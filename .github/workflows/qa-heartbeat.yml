name: Legacy workflow disabled — QA Heartbeat

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled legacy workflow
        run: |
          echo "This historical QA heartbeat is intentionally disabled."
          echo "No repository write, Pages update, external dispatch, token use, or current Axiom health claim is authorised from this legacy repository."
